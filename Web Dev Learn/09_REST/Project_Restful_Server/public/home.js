let btn = document.querySelector('button');

btn.addEventListener('click',()=>{
    window.location.href = 'http://localhost:8080/posts';  //used to redirect
});

// window.open = url(); // used to open in a new window