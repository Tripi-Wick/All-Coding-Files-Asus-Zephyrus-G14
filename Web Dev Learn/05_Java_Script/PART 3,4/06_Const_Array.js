// It reserves the address i.e. value of the arr not the array it self so we can modify array but can totally alter it i.e. an array to a new position
const arr = [1,2,3,4];
console.log(arr);

arr.push(5);
console.log(arr);
arr.pop();
console.log(arr);

// arr = [2,3]; //cant do this
