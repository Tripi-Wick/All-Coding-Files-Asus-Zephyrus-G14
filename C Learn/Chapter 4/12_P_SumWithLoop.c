#include <stdio.h>

int main()
{
    int n,a=1;

    printf("Enter the numbers whose Sum You wanna see "); scanf("%d",&n);

    // 1:- Sum of n natural numbers by While loop:-
    // (Its sort of Wrong)
    // 
    // while(a<=n) 
    // {
    //     printf("%d\n",a*(a+1)/2); 
    //     a++;
    // }

    return 0;
}