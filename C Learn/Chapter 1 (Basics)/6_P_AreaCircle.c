# include <stdio.h>

int main()
    {
        int radius;
        float pi=3.14;

        printf("The radius of circle is %d \n",radius=7);
        printf("The area of circle is %f", pi*radius*radius);

    return 0;

    }