#include <stdio.h>

int main()
{
    printf("The size of int on my lapi is %d\n",sizeof(int));
    printf("The size of float on my lapi is %d\n",sizeof(float));
    printf("The size of double on my lapi is %d\n",sizeof(double));
    printf("The size of char on my lapi is %d\n",sizeof(char));
    return 0;
}