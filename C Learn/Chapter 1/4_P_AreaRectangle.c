# include <stdio.h>
int main() {
    
    int a,b;

    printf("The lenght of the rectangle is %d \n", a=9);
    printf("The breadth of the recentagle is %d \n", b=7);
    printf("The area of the rectangle is %d", a*b );

    return 0;

}